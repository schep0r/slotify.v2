# Codeception Test Suite Configuration
#
# Suite for unit tests with Symfony integration

actor: UnitTester
coverage: true
modules:
    enabled:
        - Asserts
        - Symfony:
            app_path: 'src'
            environment: 'test'
